NOTE. This is old code. I had developed some ad hoc interface for the PoC just to show some examples.
For now I keep it here only as a "reference" of my old work having used streamlit a few times in my life.

A complete new ui module should be implemented in the future.

This folders contains the code for the [streamlit](https://streamlit.io/) interface.
